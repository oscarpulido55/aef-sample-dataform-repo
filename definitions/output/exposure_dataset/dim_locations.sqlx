config {
    type: "table",
    description: "A table for storing locations",
    columns: {
        location_id: "Location ID as defined in the source system.",
        location_name: "The location name as defined in the source system."
    }
}

CREATE TABLE
  `${dataform.projectConfig.vars.exposure_dataset_project_id}.${dataform.projectConfig.vars.exposure_dataset_dataset_id}.dim_location` ( location_id INTEGER,
    location_name STRING )
