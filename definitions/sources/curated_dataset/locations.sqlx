config {
    type: "table",
    description: "A table for storing locations",
    columns: {
        location_id: "A unique identifier for the location as defined in the source system.",
        location_name: "The official name of the location."
    }
}

CREATE TABLE
  `${dataform.projectConfig.vars.curated_dataset_project_id}.${dataform.projectConfig.vars.curated_dataset_dataset_id}location`
  (
    location_id INTEGER,
    location_name STRING )