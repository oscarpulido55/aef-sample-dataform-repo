config {
    type: "table",
    description: "A table for storing locations",
    columns: {
        product_id: "Product ID as defined in the source system.",
        product_name: "The product name as defined in the source system."
    }
}

CREATE TABLE
  `${dataform.projectConfig.vars.curated_dataset_project_id}.${dataform.projectConfig.vars.curated_dataset_dataset_id}.product` ( product_id INTEGER,
    product_name STRING )